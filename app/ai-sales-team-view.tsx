"use client";

import { useEffect, useState } from "react";
import type { AiSalesBrief } from "../src/ai-sales-team/model";
import { canMutateCommercialData, type RevenueAccessState } from "../src/lib/auth/access";
import { AiOutreachPanel } from "./ai-outreach-panel";

export function AiSalesTeamView({ access }: { access: RevenueAccessState }) {
  const [companyName, setCompanyName] = useState("");
  const [website, setWebsite] = useState("");
  const [briefs, setBriefs] = useState<AiSalesBrief[]>([]);
  const [message, setMessage] = useState("Loading saved AI Sales Briefs…");
  const [busy, setBusy] = useState(false);
  const canEdit = canMutateCommercialData(access);
  useEffect(() => { fetch("/api/ai-sales/research").then(async (response) => { const value = await response.json(); if (!response.ok) throw new Error(value.message); setBriefs(value.briefs ?? []); setMessage(value.briefs?.length ? "Saved briefs loaded." : "No AI Sales Briefs yet."); }).catch((error) => setMessage(error instanceof Error ? error.message : "Saved briefs could not be loaded.")); }, []);
  async function research() {
    if (!companyName.trim() || !canEdit) return;
    setBusy(true); setMessage("Researching public sources and building the brief…");
    try { const response = await fetch("/api/ai-sales/research", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ companyName, website }) }); const value = await response.json(); if (!response.ok) throw new Error(value.message); setBriefs((current) => [value.brief, ...current]); setCompanyName(""); setWebsite(""); setMessage("AI Sales Brief saved to the Revenue Engine memory."); } catch (error) { setMessage(error instanceof Error ? error.message : "Research failed."); } finally { setBusy(false); }
  }
 return <section className="lead-intelligence-section" aria-label="AI Sales Team"><div className="section-heading"><span className="label">AI SALES TEAM · MVP V1</span><span className="muted">Public research · human review · controlled outreach</span></div><article className="intelligence-card"><div className="lead-form-grid"><label>Prospect or company name<input value={companyName} disabled={!canEdit || busy} onChange={(event) => setCompanyName(event.target.value)} placeholder="e.g. Company name" /></label><label>Website or domain<input value={website} disabled={!canEdit || busy} onChange={(event) => setWebsite(event.target.value)} placeholder="Optional public website" /></label></div><p className="card-meta">Research uses only configured provider credentials and public sources. Unknowns remain unknown.</p><p role="status">{message}</p><button className="save-button" type="button" disabled={!canEdit || busy || !companyName.trim()} onClick={() => void research()}>{busy ? "Researching…" : "Research prospect"}</button>{!canEdit ? <p className="card-meta">Viewer access is read-only.</p> : null}</article>{briefs.map((brief: any, index) => <article className="intelligence-card" key={`${brief.account_id}-${brief.created_at}-${index}`}><div className="card-top"><span className="pill">AI SALES BRIEF</span><span className="muted">{brief.territory?.code ?? "UNKNOWN"} · {brief.qualification?.fit ?? "UNKNOWN"} FIT</span></div><h2>{brief.company_summary}</h2><p>{brief.why_it_matters}</p><div className="intelligence-grid"><div><span className="label">QUALIFICATION / ICP FIT</span><strong>{brief.qualification?.fit ?? "UNKNOWN"} · {brief.qualification?.rationale ?? "Unknown"}</strong></div><div><span className="label">EVENTSUITE RELEVANCE</span><strong>{brief.eventsuite_opportunity?.relevance ?? "Unknown"}</strong></div><div><span className="label">TERRITORY / MOTION</span><strong>{brief.territory?.code ?? "UNKNOWN"} · {brief.eventsuite_opportunity?.salesMotion ?? "UNKNOWN"}</strong></div><div><span className="label">CONVERSION ROUTE</span><strong>{brief.eventsuite_opportunity?.conversionRoute ?? "UNDETERMINED"} · PROGRAM NULL / DEFERRED</strong></div></div><div className="intelligence-detail"><span className="label">PEOPLE / CONTACTS</span><p>{(brief.people ?? []).length ? brief.people.map((person: any) => `${person.name}${person.role ? ` · ${person.role}` : ""} · ${person.kind}`).join(" · ") : "No people identified from public evidence."}</p><span className="label">PAINS / USE CASES / SIGNALS</span><p>{[...(brief.pains ?? []), ...(brief.use_cases ?? []), ...(brief.signals ?? [])].join(" · ") || "Unknown / needs validation."}</p><span className="label">ACCOUNT STRATEGY</span><p>{brief.account_strategy?.positioning || "Unknown"} {brief.account_strategy?.approach || ""}</p><span className="label">NEXT BEST ACTION</span><p>{brief.next_best_action?.action ?? "Unknown"} · {brief.next_best_action?.reason ?? "Human review required."}</p></div><div className="intelligence-detail"><span className="label">FACT EVIDENCE / SOURCES</span>{(brief.facts ?? []).length ? brief.facts.map((fact: any, factIndex: number) => <p key={`fact-${factIndex}`}>FACT · {fact.claim} {fact.sourceUrl ? <a href={fact.sourceUrl} target="_blank" rel="noreferrer">{fact.sourceTitle || fact.sourceUrl}</a> : "· source unknown"} · {fact.confidence}</p>) : <p>None returned.</p>}<span className="label">INFERENCE EVIDENCE</span>{(brief.inferences ?? []).length ? brief.inferences.map((item: any, itemIndex: number) => <p key={`inference-${itemIndex}`}>INFERENCE · {item.claim} · {item.confidence}</p>) : <p>None returned; no inference was invented.</p>}</div>{(brief.unknowns ?? []).length ? <p className="card-meta">UNKNOWNS / NEEDS VALIDATION · {brief.unknowns.join(" · ")}</p> : <p className="card-meta">No additional unknowns returned.</p>}<AiOutreachPanel accountId={brief.account_id} briefId={brief.id} access={access} /></article>)}</section>;
}
